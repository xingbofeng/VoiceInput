name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test-and-package:
    name: Test and package macOS app
    runs-on: macos-14
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Show Swift toolchain
        run: swift --version

      - name: Run tests
        run: swift test

      - name: Build with warnings as errors
        run: swift build -c debug -Xswiftc -warnings-as-errors

      - name: Build release DMG
        run: make dmg

      - name: Verify bundle and DMG
        run: |
          codesign --verify --deep --strict .build/VoiceInputApp.app
          lipo .build/VoiceInputApp.app/Contents/MacOS/VoiceInputApp -verify_arch arm64 x86_64
          test -f .build/VoiceInputApp.app/Contents/Resources/AppIcon.icns
          test -f dist/VoiceInput-1.0.0-macOS.dmg
          shasum -a 256 -c dist/VoiceInput-1.0.0-macOS.dmg.sha256

      - name: Upload macOS DMG
        uses: actions/upload-artifact@v4
        with:
          name: VoiceInput-macOS
          path: |
            dist/VoiceInput-1.0.0-macOS.dmg
            dist/VoiceInput-1.0.0-macOS.dmg.sha256
          if-no-files-found: error
